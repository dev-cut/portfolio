# Resume 배경 텍스트 크기 조정

## 작업 개요

사용자의 요청에 따라 Resume 섹션의 배경 텍스트("RESUME") 크기를 줄였습니다.

## 작업 상세

- [x] `components/ResumeGrid.module.scss`:
  - `.bgText`의 `font-size`를 `10rem`에서 `6rem`으로 축소

## 결과

배경 텍스트가 과도하게 시선을 뺏지 않도록 크기를 조정하여 전체적인 조화를 개선했습니다.
